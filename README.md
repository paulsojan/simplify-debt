## Simplify debt

A Ruby program to simplify shared group expenses. It reads multiple text files with expense details, calculates each member’s balance, and outputs minimal transactions required to settle debts among the group.

To run: `ruby simplify_debts.rb`
